// const numbers = [9, 10, 11, 12, 8, 13];

// function getBigNumber(number) {
//   if (number > 8) {
//     return number;
//   }
// }

// const result = numbers.find(getBigNumber);

// console.log(result); // 11

const updateDinoName = (name) => {
  // name = 't-rex';
  // return name;
  console.log(name);
};

let dinoName = 'brachio';

window.updateDinoName(dinoName); // call function

// console.log(updateDinoName(dinoName));
